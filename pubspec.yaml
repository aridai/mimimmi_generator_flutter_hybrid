name: mimimmi_generator_flutter_hybrid
description: ミミッミジェネレータ
publish_to: 'none'
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  rxdart: ^0.26.0

dev_dependencies:
  test: ^1.17.2

flutter:
  assets:
    - assets/mimimmi.png
